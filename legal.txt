Your personal action items — the agent cannot do these:

Register with ODPC — odpc.go.ke — mandatory for a health data app, free to register
Trademark your app name — Kenya Industrial Property Institute (KIPI) — kipi.go.ke — protects your brand
Have a Kenyan lawyer review the legal pages before launch — the agent will produce solid drafts but a lawyer must verify them. Cost is typically KSh 5,000–15,000 for a review
DPIA (Data Protection Impact Assessment) — required before processin
g sensitive health data at scale — your lawyer can help with this






All 16 phases are complete. The project is fully built and locally tested. The logical next step is      
  Railway deployment. Here's what's needed, in order:
                                                                                                             ---                                                                                                        1. Deploy backend to Railway                                                                             
  - Create a Railway project, connect the GitHub repo                                                      
  - Set all production env vars (copy from .env, replace placeholders):
    - DATABASE_URL → your Supabase connection string
    - JWT_SECRET, JWT_REFRESH_SECRET, ENCRYPTION_KEY → already generated
    - GROQ_API_KEY → already set
    - RESEND_API_KEY + EMAIL_FROM → already set
    - UPSTASH_REDIS_REST_URL + UPSTASH_REDIS_REST_TOKEN → already set
    - UPSTASH_REDIS_URL → re-enable this on Railway (port 6380 works there)
    - FRONTEND_URL → your Vite/Vercel URL (once deployed)
    - NODE_ENV=production
  - Set root directory to src/backend

  2. Run seeds against production DB (one-time)
  node src/backend/scripts/seed_admin.js admin@yourdomain.com StrongPassword123!
  node src/backend/scripts/seed_groups.js admin@yourdomain.com
  node src/backend/scripts/seed_articles.js admin@yourdomain.com

  3. Deploy frontend to Vercel/Netlify
  - Set VITE_API_URL to your Railway backend URL
  - Build command: npm run build in src/frontend

  4. After deploy — smoke test
  - Register → verify email → consent → persona → first mood → dashboard
  - Password reset end-to-end
  - Paystack purchase (needs live key — PAYSTACK_SECRET_KEY)

  5. Still pending credentials
  - Paystack live key (purchase flow)
  - Firebase FCM service account JSON (push notifications)
  - Replace [Your Name] placeholder in legal pages before going public

  Want to start the Railway deployment, or tackle one of the pending credentials first?


